package main

import (
	"fmt"
)

func main() {
	// scanner := bufio.NewScanner(os.Stdin)
	// scanner.Scan()
	// input := scanner.Text()
	// for i := len(input) - 1; i >= 0; i-- {
	// 	fmt.Print(string(input[i]))
	// }
	s := "hello"
	fmt.Print(s[4])
}
